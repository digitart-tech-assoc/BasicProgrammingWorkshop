### Q1-20[B]. $`指数的成長の計算`$

バクテリアが毎時間2倍に増殖するとき、初期個数から n 時間後のバクテリア個数を計算せよ。

指数的成長の式：
> $`n時間後の個数 = 初期個数 \times 2^n`$

```javascript
let initialCount = 100;  // 初期個数
let hour = 5;            // 経過時間（時間）

//WRITE ME!!

console.log("初期個数: " + initialCount);
console.log("5時間後のバクテリア個数: " + finalCount);
```

<details><summary>入力と出力</summary>

```
[in]
initialCount = 100
hour = 5

[out]
初期個数: 100
5時間後のバクテリア個数: 3200
```
</details>
